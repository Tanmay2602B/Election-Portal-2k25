{
    "name": "election-portal-root",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "dev": "start cmd /k \"cd backend && npm run dev\" && start cmd /k \"cd frontend && npm run dev\""
    }
}